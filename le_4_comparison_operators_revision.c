#include <stdio.h>

int main() {

    // comparison operators
    // ans true or false
    //  ==  // equal to
    //  !=  // not equal to
    //  >   // greater than
    //  <   // less than
    //  >=  // greater than or equal to
    //  <=  // less than or equal to
    
    //true and false are represented by 1 and 0 respectively and boolean data type is not available in C programming language.

    // <  less than operator

    int a = 46;
    int b = 45;
    int c = 48;

    //c = a < b; // true or false // 1 or 0
    //printf("c = %d", c);

    // <= less than or equal to operator
    //c = a <= b;
    //printf("c = %d", c);

    // > greater than operator
    // c = a > b;
    // printf("c = %d", c);

    // >= greater than or equal to operator
    // c = a >= b;
    // printf("c = %d", c);

    // == equal to operator
    // c = a == b;
    // printf("c = %d", c);

    // != not equal to operator
    // c = a != b;
    // printf("c = %d", c);

    // int r = a > b < c; 
    // printf("r = %d", r);
    return 0;
}