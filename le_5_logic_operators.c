#include <stdio.h>

int main() {

    // logical operators
    // && // and
    // || // or
    // ! // not

    int a = 5;
    int b = 10;
    int c = 15;

    // &&  logical and operator  if all conditions are true then result is true esle false
    // int r = (a < b) && (b > c) && (c < b);

    // printf("r : %d\n",r);

    // ||  logical and operator  if any one condition is true then result is true else false
    // int r = (a > b) || (b > c) || (c < b);

    // printf("r : %d\n",r);

    // !  logical and operator  if condition is true then result is false else true
    // int r = !(a < b);

    // printf("r : %d\n",r);

    // int r = (a < b) && (b > c) || (c > b);

    // printf("r : %d\n",r);

    return 0;
}