#include <stdio.h>

int main() {
    // initialization and assign value to a variable
    int a = 10,x,y;
    x = 20;
    float b = 25.65;
    char c = 'A';

    y = a + x;
    // +
    // -
    // *
    // /

    printf("The value of a is %d\n", a);
    printf("The value of b is %f\n", b);
    printf("The value of c is %c\n", c);

    printf("The value of a + x is %d\n", y);

    return 0;
}